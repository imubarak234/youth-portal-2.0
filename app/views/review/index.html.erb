<!DOCTYPE html>
<html>
  <head>
    <title>YouthPortal</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <%= yield %>
   
    <h2>Review</h2>

    <h3 class="h3">Members</h3>
    <% if @member != nil %>
      
      <div>
        <table class="table">
          <thead>
            <tr>
              <th scope="col">#</th>
              <th scope="col">Full Name</th>
              <th scope="col">Email@</th>
              <th scope="col">Member Role</th>
              <th scope="col">Phone Number</th>
              <th scope="col">Active Region</th>
            </tr>
          </thead>
          <tbody>
            <% @member.each_with_index do |n, idx| %>
              <tr>
                <th scope="row"><%= idx + 1 %></th>
                <td><%= n.full_name %></td>
                <td><%= n.email %></td>
                <td><%= n.member_role %></td>
                <td><%= n.phone_number %></td>
                <td><%= n.active_region %></td>
              </tr>
            <% end %>
          </tbody>
        </table>
      </div>
      
    <% end %>

    <table class="table table-striped table-hover">
      <thead>
        <tr>
          <th scope="col">Group Information</th>
          <th scope="col">First</th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <th scope="row">Group Name</th>
          <td><%= current_user.name%></td> 
        </tr>
        <tr>
          <th scope="row">Email Address</th>
          <td><%= current_user.email%></td>
        </tr>
        <tr>
          <th scope="row">Year of Establishment</th>
          <td>
              <% if @group != nil%>
                <%= @group.year_of_establishment %>
              <% else %>
                ...
              <% end %>
          </td>
        </tr>
        <tr>
          <th scope="row">Nature of Support Group</th>
          <td>
              <% if @group != nil%>
                <%= @group.nature_of_support_group %>
              <% else %>
                ...
              <% end %>
          </td>
        </tr>
        <tr>
          <th scope="row">Mode of Funding</th>
          <td>
              <% if @group != nil%>
                <%= @group.mode_of_funding %>
              <% else %>
                ...
              <% end %>
          </td>
        </tr>
        <tr>
          <th scope="row">Acive Region</th>
          <td>
              <% if @group != nil%>
                <%= @group.active_region %>
              <% else %>
                ...
              <% end %>
          </td>
        </tr>
        <tr>
          <th scope="row">Strength of group(In figures)</th>
          <td>
              <% if @group != nil%>
                <%= @group.number_of_members %>
              <% else %>
                ...
              <% end %>
          </td>
        </tr>
        <tr>
          <th scope="row">Nature of Support Group</th>
          <td>
              <% if @group != nil%>
                <%= @group.nature_of_support_group %>
              <% else %>
                ...
              <% end %>
          </td>
        </tr>
      </tbody>
    </table>
  </body>
</html>
